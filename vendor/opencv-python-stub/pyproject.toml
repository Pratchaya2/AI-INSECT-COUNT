[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "opencv-python"
version = "4.10.0.84"
description = "Empty stub satisfying ultralytics' opencv-python requirement so opencv-python-headless provides the real cv2 module"
requires-python = ">=3.8"

[tool.setuptools]
packages = []
